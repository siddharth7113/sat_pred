# @package _global_

# specify here default training configuration
defaults:
  - _self_
  - trainer: default.yaml
  - model: simvp.yaml
  - datamodule: default.yaml
  - callbacks: default.yaml
  - logger: wandb.yaml
  - hydra: default.yaml

# Name of the model run used on wandb
model_name: "unnamed_model"

seed: 27267831
